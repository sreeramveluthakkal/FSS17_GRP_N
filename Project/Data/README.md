# Datasets

## File names:
* `{{dataset_name}}.csv`
    * The original data
* `{{dataset_name}}_m.csv`
    * The mutated data
* `{{dataset_name}}_both.csv`
    * both original and mutated data in one file. original on top.
    